# ACE known-bug #10 (generic/348): symlink into a dir, fsync the DIR —
# the symlink entry must survive (uses the converter's symlink
# extension; the link target string does not need to resolve).

# define
test
A
foo
A/bar

# run
mkdir A 0777
creat foo 0777
sync
checkpoint 1
symlink foo Abar
fsync A
checkpoint 2
