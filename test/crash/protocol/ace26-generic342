# ACE known-bug #26 (generic/342): synced write, rename, recreate and
# rewrite the old name, fsync it — old content must be at the new name
# or the op absent, never crossed.

# define
test
foo
bar

# run
creat foo 0777
write foo 0 4096
sync
checkpoint 1
rename foo bar
write foo 0 4096
fsync foo
checkpoint 2
